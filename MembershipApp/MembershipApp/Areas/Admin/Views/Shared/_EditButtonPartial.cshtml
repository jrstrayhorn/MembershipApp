@model int

<a href="@Url.Action("Edit", new { id = Model })" class="btn btn-primary btn-sm" type="button">
    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
    <span>Edit</span>
</a>
